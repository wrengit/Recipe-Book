$(document).ready(()=>{$(".recipe-delete-confirm").on("click",e=>{recipeId=$(e.target).data("id");if(confirm("Are you sure you want to delete this recipe?")){location.href=`/deleterecipe/${ recipeId }`}});$("#flash").delay(4000).fadeOut()});